# GUIDE · 用 Claude Code 学 C++ 的方法论

> 这是「怎么学」的说明书（给人看的）。Claude 遵循的硬性规则见 `CLAUDE.md`。

## 一、为什么用「对话推进」而不是一本写完的教程

传统教程一次性写完，问题在于：

1. **进度固定**：无法根据你的薄弱点调整。
2. **练习靠自觉**：没有机制逼你动手。
3. **错误反馈慢**：卡住只能自己搜。

本项目把学习拆成**一次一次会话**，每次 Claude 只生成一小步内容，你亲手练习后回来 review。节奏、深度、侧重点都由你的**真实表现**驱动 —— 这就是「自适应」。

## 二、一次学习会话长什么样

### 会话 A：生成新模块

```text
你：继续学习
Claude: 阅读 ROADMAP + PROGRESS
        → 生成 docs/03、examples/03、practice/03
        → 编译验证所有示例
        → 告诉你：本节目标、看哪些示例、练哪些题
你：去 practice/03 写练习
```

### 会话 B：复习 / 卡住求助

```text
你：我卡在练习 3 了
Claude: 读你的代码 → 编译运行 → 指出问题但不直接给答案（先引导）
        → 必要时给提示
```

### 会话 C：review 交作业

```text
你：我写完了，帮我 review
Claude: 编译运行你的练习 → 逐条反馈（对在哪/能改哪/达标没）
        → 更新 ROADMAP + PROGRESS → 建议下一步
```

### 会话 D：调整路线

```text
你：指针这块我太弱，加两周练习 / 或者「我准备面试，把 M7 提前」
Claude: 直接改 ROADMAP.md，重新排序/拆细/精简
```

## 三、高效提问模板

不限定句式，但带上下文的提问效果最好：

- 「继续学习」—— 最简单，Claude 自己接管
- 「开始模块 M5 现代 C++，我最近在准备面试」—— 带目标，Claude 会调整侧重点
- 「练习 02 的第 3 题我这样写：……（贴代码），运行结果是……，为什么？」—— 贴代码 + 报错，反馈最准
- 「这段代码为什么输出垃圾值？」—— 不指定文件的话 Claude 会让你贴/定位

## 四、自适应机制 | How the roadmap adapts

`ROADMAP.md` 是「活的计划」：

- ✅ 每完成一个模块，状态更新
- 🔍 自测/练习暴露薄弱 → 模块被**拆细**（加小节、加练习）
- 🚀 掌握良好 → 模块被**精简/跳过**
- 🔀 目标变化（比如突然要面试）→ 模块**重排/增删**

规则只有一条：**以你的真实表现为准，而不是原计划的顺序。** 你可以随时对 Claude 说「调整路线」。

## 五、练习的正确姿势 | How to practice

1. **先自己写**，卡 15 分钟再求助 —— 卡壳本身就是学习。
2. **别急着看 `solutions/`**。参考答案放那儿是让你写完**之后**对照的。
3. **改出点花样**：参考答案之外，试试别的写法，然后问 Claude「两种写法的区别」。
4. **编译错误是老师**：把报错贴给 Claude，让它解释报错信息的意思。

## 六、推荐的日常节奏

节奏随缘没问题，但记住一条：**小步、高频、不断点**。

- 一次会话只推一个模块（甚至模块的一小节）
- 每次会话结束 Claude 都会写进度日志，下次回来无缝续接
- 如果间隔很久才回来，先让 Claude「回顾一下我学到哪了」

## 七、进度与承诺

- 每完成一次 review，Claude 必须更新 `ROADMAP.md` + `progress/PROGRESS.md`。
- 你也可以随时打开这两个文件自己看进度。

> 记住：**示例和文档是 Claude 写的，练习是你写的。** 这个分工是你学到的关键。
