// practice/03/04_string_ops.cpp — M3 练习4：string 字符串处理
// 输入：一行英文句子（可能含空格，用 getline）
// 输出：单词个数 / 最长单词（并列取第一个）/ 反转后的整个句子
// 提示：数单词用 <sstream> 的 std::istringstream（>> 按空白切分）；
//       反转用反向迭代器 std::string rev(line.rbegin(), line.rend());
#include <iostream>
#include <string>

int main() {
  // TODO: 你的实现

  return 0;
}
